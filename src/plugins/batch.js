// batch save/destroy
// batch get by ary of keys
Lawnchair.plugin({

    saveEach: function (keys, callback) {

    },

    // overrides default get behavior to accept array of keys
    get: function (keys, callback) {

    },

    remove: function() {

    }
})
